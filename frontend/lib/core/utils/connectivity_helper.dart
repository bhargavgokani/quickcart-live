class ConnectivityHelper {
  /// Check if device is connected to the internet.
  /// Currently a placeholder for future connectivity integration.
  static Future<bool> hasInternetConnection() async {
    return true;
  }
}

'use strict';

/**
 * User roles used throughout the application.
 * Import this constant wherever a role string is needed –
 * never hardcode 'ADMIN' or 'CUSTOMER' directly.
 */
const ROLES = {
  ADMIN: 'ADMIN',
  CUSTOMER: 'CUSTOMER',
};

module.exports = ROLES;
